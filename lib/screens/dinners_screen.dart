import 'package:flutter/material.dart';

class DinnersScreen extends StatelessWidget {
  const DinnersScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('Dinners Screen');
  }
}
